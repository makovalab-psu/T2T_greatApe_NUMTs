paths:
  # The root directory for your processed data
  scratch_base: "/scratch/ejt89/apes_short_reads"
  
  # Directory for fasterq-dump temporary files (ensure this exists)
  tmp_dir: "/scratch/ejt89/tmp"
  
  # Where Slurm .out and .err files will be stored
  log_dir: "logs/slurm"

# The aggregated sample metadata file generated by get_tsv_metadata.py
samples_metadata: "config/samples.yaml"

